[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "graphrag-plus-plus"
version = "0.1.0"
requires-python = ">=3.9"
description = "Hybrid RAG system combining a knowledge graph with dense + sparse passage retrieval"
